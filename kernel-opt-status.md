# Kernel-Opt Loop — Status

State file for `guides/kernel-opt-loop.md`. One row per item in execution order; add
sub-rows (`14.1`, `14.3`, …) for multi-phase plans. Measurements live in
`benchmarks/kernel-opt.md` — this file records **state and disposition**, and points at the
run label.

Dispositions: `PENDING` / `IN-PROGRESS` / `KEPT-ON` / `LANDED` / `KEPT-DARK` / `PARTIAL` /
`REVERTED` / `BLOCKED`.

## Baseline

| quantity | value | source |
|---|---|---|
| `baseline_wall` | **3:31.59** (r1 3:33.92 / r2 3:29.25 mean); live spread 4.67 s ≈ 2.2% | Run D — loop-entry baseline |
| `baseline_counters` | 379,486,685 obj / 18,524.03 MB / 862 minor / 372,250,555 promoted (98.1%) / 10 major; RSS 5,111,732 kB; out.mlir 12,943,401 B | Run D |
| baseline binary | `build/compiler/build-kernel/bin/eco-kopt-base` (2026-08-10 19:53) | setup step 1 |
| `--target full` green count | **1632 / 1632, 0 failed** (~4 min per run) | measured 2026-08-10 20:20 |
| heap-validate green count | **not measured — gate removed from the loop** (user decision 2026-08-10). See `guides/kernel-opt-loop.md` §"Gates this loop deliberately does NOT run" for what that gives up. | |
| bootstrap fixed point | **removed from per-item gating**; one run at DONE only | |
| `elm-tests` count | **13066 passed / 12 failed** — the suite is **already red on the pristine tree**; all 12 are TYPE_007 constraint-generation suites + one golden fingerprint, none related to this track. "Green" for this gate means *still exactly these 12*. | pre-change run 2026-08-10 20:02 |
| series entry point | Run C (one-call Order materialization, 3:34.71) | `benchmarks/kernel-opt.md` |

Run D reproduces Run C's counters bit-for-bit (1-object jitter) and its `out.mlir` byte
size, so the tree is unmoved and the baseline is trustworthy.

Re-point `baseline_wall` / `baseline_counters` / `eco-kopt-base` after every **KEPT-ON** item.

## Items

| order | item | disposition | run | Δwall | counter deltas | notes |
|---|---|---|---|---|---|---|
| 1 | 07 kernel-facts-table | **LANDED** | E | −1.30% (FLAT) | objects identical; promoted +63 of 372M; minor/major identical | 52 rows, shim, 7 suites, `Utils_equal` trace deleted. **`out.mlir` byte-identical pre vs post on a frozen 243-module workload** ⇒ inert, proven. E2E 1632/1632; elm-tests 13066→13073 (+7 new suites), 12 pre-existing failures unchanged. Binary +173,400 B (plan predicted a shrink — wrong). Anchor rot from the deletion fixed: 11 `Utils.cpp` anchors re-pinned. |
| 2 | 01 list-cons → construct.list | **KEPT-ON** (`ECO_LIST_CONS_INTRINSIC=0` escapes) | F | +0.36% (FLAT) | promoted/minor/major identical; `Objects allocated` −38.7% is HEAP_034 counter blindness, **not** deleted allocation | **All 4,304 kernel cons sites → 0, zero declines.** EcoListTemplate parity bit-identical (rewritten=444, consRoots=0). Flag-off output byte-identical to pre-change on a frozen corpus. Gates: E2E 1633/1633 default-on; elm-tests 13073→13085 (+12 unit tests), 12 pre-existing unchanged. Plan Gate 3 was **unsatisfiable as written** (self-compile byte-identity vs an item that adds compiler source) — corrected in the plan file to a counts gate + fixed-corpus identity. Honest: billed as the series' best wall bet; ~147M dynamic kernel calls deleted for a flat wall. |
| 3 | 02 array-push churn | **KEPT** (lanes A + A′; no flag) | G | **−4.46% (REAL WIN)** | objects −5.44%, bytes −12.08%, minor 862→836, **promoted −2.96%**, GC time −6.04% | Census refuted the plan's premise (push traffic 6.3× lower than assumed) but confirmed H1 via a second stack frame: `UnionFind.fresh` = 4,739,084 calls doing 1 int + 2 box pushes. Lane A merged the 3 index-synchronised arrays into one `PointCell` array (12 files, type-checker core). **G2 byte-identity passed** — output preserved exactly. E2E 1633/1633; elm-tests unchanged at 13085/12. Also found: `Array.set` is 81% of all copying and needs its own plan; lane C is definitively closed (no growable-vector usage: max len 31/32, b33p=0). |
| 4 | 04 string.length / code_unit_at | **KEPT-ON** (`ECO_STRING_LENGTH_OP=0` escapes) | H | −0.12% (FLAT) | counters identical; minor 836, major 10, promoted equal both arms | **All 101 kernel `String_length` call sites → 0**, exact 1:1 with `eco.string.length`. Inline `header.size` load (one word serves all six String forms per HEAP_025/032) via a marker expanded at LLVM level; `ptr_ind` bit test, not `== 0x6`, so a Bool constant returns 0 instead of dereferencing address 4/5. `eco.string.code_unit_at` also lands with **no Elm emission** — it unblocks item 14's String-HOF phase. FORBID_HEAP_002 amended for the inline read. Gates: E2E **1636/1636 in both flag states**; elm-tests 13085/12 unchanged. |
| 5 | 05 Utils_append type split | **KEPT-ON** (`ECO_APPEND_SPLIT=0` escapes) | I | +0.80% (FLAT) | counters identical; minor 836, major 10, promoted equal | **3,468 kernel append sites → 67** (98.1% displaced); split reconciles exactly: 2,695 string + 706 list = 3,401. Residue is the MVar-operand population, by design. Stage-5 `.mlir` −6,773 B — the IR-size effect is the purchase the plan claims, and it is real but small. Both ops trait-free and absent from all four EcoGCPrepare lists (variable-size ⇒ RS4GC statepoints them). Phase 3 filled the `(Utils, append)` borrow axes POwned/POwned + resultAliases [0,1] and grew item 07's golden 33 → 34. Gates: E2E **1638/1638 both flag states**; elm-tests 13085/12. |
| 6 | 06 string ordering cmp3 | **KEPT-ON** (`ECO_STRING_ORDER_INTRINSIC=0` escapes) | J | −0.34% (FLAT) | counters identical; minor 836, major 10, promoted equal | **95 of 121 sites convert** (lt 79→14, gt 40→10, ge 2→2, le 0→0) against cmp3 1→96 — exact 1:1, inside the predicted 95–100. Phase-0 baseline reproduced exactly. CGEN_075 gains clause (f): the cmp3 sign is UNCLAMPED so the test is against 0 and must be SIGNED. `generateIntrinsicOps` (list-returning) added for the 3-op shape; both Expr sites use it. **Residue published for item 03: 64 boxed sites total (lt 14, le 0, gt 10, ge 2, compare 38) — under the >200 threshold, so 03's Phase 5 MUST NOT execute.** Gates: E2E 1639/1639 both flag states; elm-tests 13085/12. |
| 7 | 03 value.eq fast path | **KEPT-ON, COMPLETE** (`ECO_VALUE_EQ=0` escapes) | K | −1.84% (inside band ⇒ FLAT) | counters identical; minor 836, major 10, promoted equal | **All 7 phases done.** Census said the inline arms were worth 6.47% vs a 25% bar; the bar was overruled and the item built in full. **Emission 100%: `Utils_equal` 1392→0, `Utils_notEqual` 60→0, `eco.value.eq` +1452, exact 1:1.** P1 depth-100 golden landed; P2 op+marker+3-arm expansion; P3 emission incl. `Patterns.elm` IsStr and both synthesized string-case sites; P4 `Elm_Kernel_Utils_equal` decl now gc-leaf (A1 stampable 14, trace deleted by 07); P5 closed by 06's residue; P6 defaults + **CGEN_076**. Gates: E2E **1642/1642 in all three switch states** + default-on. `ECO_VALUE_EQ_STRCASE` also **DEFAULT-ON** (Run L, −0.22% FLAT, cheap backend-only A/B: one `.mlir` lowered twice). Nothing in this item is unmeasured. |
| 8 | 08 kernel gc-leaf stamp | **KEPT-ON** (`ECO_KERNEL_GCLEAF_EMIT=0` front-end / `ECO_KERNEL_GCLEAF=0` backend escape) | M | −1.25% (FLAT) | counters identical; minor 836, major 10, promoted equal both arms | **+2,223 de-statepointed call sites (11,950 → 14,173, +18.6%)** and +21 GC-free functions (3,688 → 3,709 of 87,327). Binary **−287,952 B (−0.439%)**, of which `.llvm_stackmaps` is −284,976 and `.text` only −2,064 — 99.0% metadata, which is *why* the wall is flat (sixth data point for that pattern). Eligibility rides on `KernelDeclInfo.gcLeaf` from the `KernelInstanceKey`, never reverse-parsed from the symbol; OR-merged in `insertKernelDecl`. **Only 10 of the 14 eligible kernels are stamped — the other 4 lost their call sites to earlier items in this very series** (`Utils_equal`/`notEqual` → item 03, `String_length` → item 04, `Utils_le` had zero sites per item 06); `Utils_equal` still gets gc-leaf directly via `attachGcLeafPassthrough` on `getOrCreateUtilsEqual`. `numDefined` is 87,327 vs the plan's pilot 44,967 (~1.94×) — unexplained, but stable across arms so it cancels. CGEN_072 gains clause (f); KERNEL_FACTS_001 added. Gates: E2E **1643/1643 in both flag states** and again after the flip; frozen-corpus `-out.mlir` byte-identical. **NOT built: the plan's GCStats audit harness** — so KERNEL_FACTS_001 was written without a harness clause, and the tripwire for a lying `GcNone` row is still owed. |
| 9 | 09 GCPrepare barrier relaxation | **PARTIAL / KEPT-ON** (Phases 0+3+2-pre landed default-on; Phases 2/2A/2B dropped. `ECO_GCPREPARE_LEAF_SAFEPOINT=0`, `ECO_GCPREPARE_SPLIT_INLINE_GROUPS=0`, `ECO_GCLEAF_MARK=0` escape) | N | −0.23% (FLAT) | minor 836, major 10 identical; promoted +8 of 361M. `Objects allocated` −0.84% / bytes −0.64% is **HEAP_034 counter blindness** (2,788 sites moved from the counted region path to the uncounted inline bump), not deleted allocation | **The census killed the plan's headline transform.** Of 2,145 crossable merge windows, **2,105 (98.1%) are blocked by a real intra-group SSA dependency and `mergeableLeaf` was exactly 0** — the gc-leaf fact unlocks no merge anywhere in the module, so design-doc §8 row 3's premise is refuted. What shipped instead: **Phase 3** — new module pass `EcoMarkGCLeafCalls` stamps `eco.callee_gc_leaf` (798 sites from 10 decls) and `EcoGCPrepare` stops rooting them: safepoints 154,323→153,525 (−798, exact), root operands −2,533; **byte-identical binary**, so it is pure compile-time. **Phase 2-pre** — a run of adjacent allocations whose members each have a call-free HEAP_034 inline lowering is no longer grouped, because grouping costs an out-of-line region call + one init call per member where the ungrouped form makes none: **1,385 groups / 2,788 objects, 4,173 out-of-line calls deleted**, binary −25,304 B (`.text` +16,192, stackmaps −40,104). New invariant **CGEN_077**; KERNEL_FACTS_001 amended. First-ever fixture for the allocation-group path (`test/codegen/gc_group_split_inline.mlir`, both directions). Gates: E2E **1643/1643** default-on and with both kill switches; whole-item flag-off byte-identical to pre-09. **Honest correction:** my first census reading claimed 16,005 groups / 44% of allocations — `runs>=2` counts adjacent runs *before* the dependency guard; the real figure is 1,385 / 3.45%. **Owed to item 12:** hoist its `eco.cse_safe` strip above the `isCallSafepoint` guard, which now short-circuits for stamped calls. |
| 10 | 11 mono DCE + kernel cost classes | **KEPT-ON** (both flags; `ECO_KERNEL_FACTS_DCE=0` / `ECO_KERNEL_COST_CLASSES=0` escape) | O | −0.50% vs base (FLAT) | minor 836, major 10 identical; promoted −0.10%, objects −0.01% | **Census first, and it is tiny: the DCE widening's realizable ceiling on the whole 261-module self-compile is FOUR sites, of which 2 realize** (`deadLets=450 deadDroppableKernelLets=4`; `letDCE` 498->500, `kernelLetDCE=2` -- the gap is argument impurity, exactly as predicted). (a) ships for the enabling value and for ending the row-5 contradiction (`isPureExpr` says every kernel call is impure while `CafHoist`/`CafDedupe` treat non-Debug kernel exprs as pure enough to hoist/merge), NOT for a win. **Decision D-K settled by measurement: `deadBareKernelVar = 0/450`** -> bare kernel vars stay impure-conservative, no follow-up owed. (b) cost classes DO move real inlining: emitted `.mlir` +1,341 B, `letDCE` 498->441. **Phase 1 found the plan's draft D-3 to be FALSE and it was corrected before landing**: named `let` bindings run before wildcard statements and not in source order among themselves (measured `n2, n1, w1, w2`), so source order is NOT preserved in general -- wildcard-only sequences are, which is why all 558 fixtures missed it. Policy now states the truth plus a do-not-worsen rule; pinned by TWO fixtures (`DebugLogOrderingTest` for D-3 over wildcards, `DebugLogNoDropTest` for D-1) rather than one mixed file, and the mandatory break-on-purpose negative control passed. New invariant `OPT_DEBUG_ORDER_001`. The `GlobalOpt -> Generate.MLIR.Intrinsics` import (first such edge in the tree) compiles, so the config-predicate fallback was not needed. **Flag-off byte-identical to the item-09 baseline on the frozen corpus, both rounds.** Gates: E2E **1646/1646** in both flag states and again after the flip; elm-tests 13085/12 unchanged. **Owed:** the per-constant A/B for (b)'s four knobs -- the vector 1/4/8/20 is the audit-implied shape, not a measured optimum. |
| 11 | 13 mono-level CSE of pure calls | **KEPT-DARK** (landed default-OFF; `ECO_CSE=1` enables) | P | −1.71% (FLAT) | **objects +1.66%, bytes +1.23%** — the pass's own analysis cost, not emitted code; minor 836→838, major 10, promoted −0.02% | **D-C gate FAILED by 40×**: `nearShareBp=5` against a required 200. C2 built and benchmarked anyway per the loop rule. **The census was wrong the first time** — every decider `Leaf (Inline _)` shared one path key, inflating `nearRedundant` to 1619 vs the true **82**; the same defect made the transform emit a `MonoLet` that did not dominate its uses. Corrected census and transform now agree exactly at 82, which is what makes the number trustworthy. **Second defect, caught by a fixture**: the scope test missed `MonoTailDef` PARAMETERS, so a candidate was hoisted above its binder (`MultiLocalTailRecTest`, `lookupVar: unbound variable i`). Third hazard fixed pre-emptively: `dropOverlapping`, since a group's let binds the original subtree verbatim. **`b2_branch`=1672 of 1909 (87.6%)** — essentially all Elm-level redundancy here is guarded on both sides (C4 speculation territory); the probe-then-insert idiom the plan is named for is 54 occurrences in 147,860 call sites. `loopNear=5`. **Stays OFF unlike every other item**: this is the first where flag-on has a measured cost (+3.6M allocations to delete 81 static occurrences). New invariant `CSE_001`. Gates: E2E **1646/1646 in both flag states**. |
| 12 | 10 MLIR CSE + project-of-construct folder | **SPLIT: folder KEPT-ON** (`ECO_MLIR_FOLD=0` escapes) **/ CSE KEPT-DARK** (`ECO_MLIR_CSE=1` enables; a default-on attempt at item 13 was REVERTED — the NaN-sharing miscompile, see Run R) | Q | folder +0.76% (FLAT); both-arm +2.66% | folder-only counters BIT-EQUAL to off (promoted +1 of 358M, majors 10). **CSE: promoted +1.56%, RSS +6.9%, majors 10→11, GC time +7% — the C-R1 live-range stretch, plan outcome (c), NO-GO by its own rule.** Composition super-additive: folds make code identical, CSE merges it, merged values live longer. | Census extension first (user instruction): get_tag-of-construct 443 (built — fired once dynamically; static sites restructure before M4), unbox-of-box 62 (skipped), `value.eq %a,%a` 0 (dead). Folder = 2,381 project folds + 1 tag fold, exe −4,096 B, zero compile-time cost. **Latent `EcoListCursor` bug found and fixed**: per-RESULT `hasOneUse` admits an interior scf.if shared by two walk positions; double rebuild dangles the first walk's saved idx Value → SEGV. Untriggerable pre-item; fixed with a treeOps disjointness bail. List folds dark behind `ECO_MLIR_FOLD_LIST` (0 dynamic hits). `num-cse'd` stats aggregate to 0 under the nested parallel pipeline — volume proved by exe deltas (cse-only −175,864 B). Barrier fixtures made fold-proof (function-boundary container). Gates: E2E **1648/1648 both flag states**; codegen 378/378. |
| 13 | 12 eco.call purity attr | **KEPT-ON** (`ECO_CALL_PURITY=0` escapes) | R | FLAT (binary byte-identical with CSE off) | counters bit-identical in every attr-vs-no-attr pair | **S=4,330 stamped** of 85,437 calls (plan predicted ~10,400 — items 01/03/05 ate the pool; third occurrence of the series consuming its own opportunity). Mirroring audit passed incl. `MVar_put`/`Scheduler_*` (they CONSTRUCT task descriptions; `auditedPure` is correct). **2×2 vs MLIR CSE: the attr is free and buys ~nothing today** — CSE off: byte-identical binary; CSE on: exe −16 KB, counters bit-identical. On the PRE-series corpus the attr alone DCE'd 1.1 MB of exe (Trap F is real, corpus-dependent). **Side-finding: CSE's retention effect swings sign across artifacts** (+5.6M promoted in Run Q vs −3.64M here, same workload, wall FLAT in both) — the user flipped `ECO_MLIR_CSE` on under keep-if-wall-flat, and the full-battery gate under the new default then **failed 3 Float container-equality tests — a real miscompile**: CSE merges NaN-containing allocations into one object and the kernel's pointer-eq fast path answers True where structural equality demands False. **REVERTED same day.** Object identity is observable in Elm via NaN; kernel-opt-10's audit parenthetical is false; the hazard is latent in dark Mono CSE too (recorded in CSE_001). Sound enablement = Allocate-on-result effects + a no-Float-in-result KernelFacts axis. One race DISCARDED as invalid (attr arms lowered from the pristine-corpus compile = pre-series compiler; redone from one current-tree artifact). Item-09 hand-off discharged: strip before `isCallSafepoint`, pinned by fixture + validator audit. Effects interface conservative branch verified by F2 (green before and after). Gates: E2E **1656/1656 in both flag states and after the flip**; 8 new fixtures. |
| 14 | 14 Elm-source List HOFs | PENDING | — | — | — | per-phase rows; symbol deletions are a separate step |

## Log

*(append one dated line per state change: item, what happened, gate counts, run label)*
