# elm-coverage relies on binwrap install scripts to fetch elm-instrument.
# Re-enable install scripts here so the parent compiler/.npmrc (which
# disables them for the main pnpm install) does not break elm-coverage.
ignore-scripts=false
